name: Deploy Wiki

on:
  push:
    branches: [publish]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Deploy to Cloudflare
        run: npx wrangler deploy

permissions:
  contents: read
  deployments: write
