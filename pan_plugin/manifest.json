{
  "_concepts": {
    "profile": "Character 创建模板。Pan 用 profile 的 adapter/model/mcp_servers/system_prompt 创建 character，创建后 character 独立运行，拥有唯一记忆和多个 session。",
    "character": "独立机器人实例，由 profile 创建。持有唯一记忆，可挂多个 session。同一 profile 可创建多个同规格、各自记忆的 character。",
    "session": "character 下的一个对话房间（绑 group_id + character_id）。session 内需计算时才 spawn Worker，Worker 无状态无个性。",
    "mcp_servers": "此 profile 创建的 character 绑定的外部工具（RuleWhisper MCP）。注入给 character 的 session，LLM 经 MCP 调用 RuleWhisper 的全部能力——规则检索、骰子、结构化数据查询、game/char 管理、版本管理。唯一不暴露的是 rebuild_index（运维操作）。",
    "_mcp_tools": [
      "query_rule", "roll_dice",
      "get_weapon", "get_monster", "get_spell", "get_skill",
      "game_create", "game_list", "game_info", "game_bind",
      "char_create", "char_list", "char_show", "char_update",
      "version_list", "version_info"
    ],
    "command_routes": "确定性指令路由，群消息前缀匹配后直发 HTTP API，不经过 character/Worker/LLM，毫秒级响应。"
  },
  "name": "rulewhisper",
  "description": "COC 跑团助手 — 规则检索 + 骰子引擎",
  "health_check": "http://127.0.0.1:9731/api/health",

  "command_routes": [
    {
      "prefixes": [".rc", ".ra", ".rb", ".rp", ".rs", ".sc", ".dam"],
      "target": "http://127.0.0.1:9731/api/dice",
      "method": "POST",
      "strip_prefix": true
    },
    {
      "prefixes": [".coc", ".rule"],
      "target": "http://127.0.0.1:9731/api/query",
      "method": "POST",
      "strip_prefix": true
    }
  ],

  "mcp_servers": [
    {
      "name": "rulewhisper",
      "command": "python",
      "args": ["-m", "src.server.mcp"],
      "cwd": "${PLUGIN_DIR}"
    }
  ],

  "profiles": [
    {
      "name": "coc-keeper",
      "adapter": "cbc",
      "model": "hy3",
      "permission_mode": "bypassPermissions",
      "mcp_servers": ["rulewhisper"],
      "system_prompt": [
        "你是 COC 守秘人(KP)，用中文回复。",
        "所有规则查询、骰子检定、数据查询、游戏和角色卡管理都通过 RuleWhisper 工具进行，绝不自己编数据。",
        "检定结果需展示公式与最终值。",
        "创建跑团前先用 game_create 建团，再按需用 char_create 添加调查员。"
      ]
    }
  ]
}
