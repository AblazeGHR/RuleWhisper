# P3: COC 7th 规则提取任务

## 目标

从守秘人规则书 `data/守秘人规则书.txt` 中，用 LLM 按章节提取所有**可执行的游戏机制规则**，输出为 `data/rules.json`。

核心理念：规则书的文本 = 70% 叙事胶水（故事、范例、GM 建议）+ 30% 机械规则（判定流程、数值公式）。只提取后者。

---

## 输出格式

每条规则一条 JSON：

```json
{
  "id": "sanity_threshold_temp_insanity",
  "章节": "第八章 理智",
  "页码": 131,
  "标签": ["理智", "临时性疯狂", "阈值"],
  "触发条件": "调查员一次性损失5点或更多理智值",
  "机制效果": "进入临时性疯狂状态，持续 1D10 小时",
  "相关检定": ["INT检定"],
  "判定流程": ["1. 计算本次理智损失", "2. 若>=5点，触发临时性疯狂", "3. 骰1D10确定持续时间"],
  "数值": { "触发阈值": 5, "持续时间": "1D10小时" },
  "原文引用": "如果调查员一次性损失了5点或更多理智值..."
}
```

**字段说明：**
- `id`: 英文标识，snake_case
- `章节`: 所在章节名
- `页码`: 规则书原文页码（txt 页码 = 给定页码 + 1）
- `标签`: 1-5个关键词标签，用于搜索
- `触发条件`: 什么情况下触发这条规则（可省略，如果规则是恒常的）
- `机制效果`: 这条规则造成什么结果
- `相关检定`: 涉及的检定类型（可省略）
- `判定流程`: 按步骤描述的判定过程（可省略）
- `数值`: 提取出的数字常量（可省略）
- `原文引用`: 规则出处的原文摘录（用于验证）

---

## 资源文件

| 文件 | 位置 | 说明 |
|------|------|------|
| 规则书全文 | `data/守秘人规则书.txt` | 1.5MB，UTF-8，每页 `===== 第 X 页 =====` |
| 输出 | `data/rules.json` | 提取结果 |
| Prompt 目录 | `prompts/` | 存放各章节的提取 prompt |

---

## 各章节页码（按提取优先级）

| 优先级 | 章节 | txt 页码范围 | 预计规则数 | 说明 |
|--------|------|-------------|-----------|------|
| 1 | 第八章 理智 | 130-143 | 15-20 | 理智损失、疯狂判定、恢复——查询频率最高 |
| 2 | 第六章 战斗 | 86-111 | 20-30 | 伤害、战技、射击、HP 归零 |
| 3 | 第五章 游戏系统 | 72-85 | 15-20 | 检定系统、难度等级、奖励骰/惩罚骰 |
| 4 | 第七章 追逐 | 112-129 | 10-15 | 追逐规则、MOV、险境 |
| 5 | 第四章 技能 | 44-70 | 10-15 | 孤注一掷、对抗检定、技能使用规则 |
| 6 | 第三章 创建调查员 | 24-43 | 5-10 | 属性生成、职业、技能点分配 |
| 7 | 第九章 魔法 | 144-152 | 10-15 | 施法消耗、POW 牺牲、法术学习 |
| 8 | 第十六章 附录 | 356-392 | 20-30 | 伤害类型表、疯狂症状表、武器物品价格 |

---

## 提取规则

### 应该提取（YES）

1. **判定规则**: "掷百分骰，结果≤技能值即为成功"
2. **数值公式**: "伤害加值 = (STR+SIZ) 查表"、"生命值 = (CON+SIZ)/10"
3. **阈值条件**: "理智值一次性损失5点或更多"、"生命值归零时进入濒死"
4. **流程步骤**: "进入战斗 → 1. 宣告行动 → 2. 敏捷排序 → 3. ..."
5. **状态机变更**: "临时性疯狂持续 1D10 小时后进入潜在疯狂"
6. **表格引用**: "近战武器伤害见第 220 页武器表"——记录为引用 + 描述

### 不应该提取（NO）

1. **故事案例**: "哈维正在大街上追逐邪教徒..."
2. **GM 建议**: "守秘人可以根据情况调整难度..."
3. **世界观描述**: "克苏鲁神话起源于洛夫克拉夫特..."
4. **纯角色扮演引导**: "玩家的调查员可以自由发挥..."
5. **重复表述**: 同一规则多次出现时只取最完整的一条

---

## 执行方法

建议分两步走：

### Step 1: 单章试做（第八章 理智）

读入第八章全文（`data/守秘人规则书.txt` 第 130-143 页），手动尝试提取。验证：
- 输出格式是否合理
- 能否覆盖该章所有重要机械规则
- 调整 prompt 和输出格式

### Step 2: 批量提取

基于验证后的方案，逐章提取。每章独立执行，输出追加到 `data/rules.json`。

示例 prompt（精炼版）：

```
你是一个 COC 7th 规则解析器。从以下规则书文本中提取所有**可执行的游戏机制规则**。

要求：
- 忽略故事案例、GM 建议、世界观描述
- 只提取判定流程、数值公式、阈值条件、状态变更等机械性规则
- 每条规则输出为 JSON

输出格式示例：
{
  "id": "规则英文标识",
  "章节": "第八章 理智",
  "标签": ["理智", "疯狂"],
  "触发条件": "调查员一次性损失5点或更多理智值",
  "机制效果": "进入临时性疯狂，持续1D10小时"
}

现在处理以下文本：

[粘贴章节文本]
```

---

## 验收标准

- [ ] 覆盖第八章（理智）全部核心规则
- [ ] 覆盖第六章（战斗）全部核心规则
- [ ] 覆盖第五章（游戏系统）全部核心规则
- [ ] 覆盖第七章（追逐）全部核心规则
- [ ] 覆盖第九章（魔法）核心规则
- [ ] 共计 60-100 条规则
- [ ] 每条规则可通过标签/关键词检索
- [ ] 输出格式一致，JSON 可解析

---

## 当前工作树状态

```
d:/project/ai_coc_p3/        ← 你在这里（分支 p3-rule-extract）
├── data/
│   ├── 守秘人规则书.txt      ← 1.5MB 规则书全文
│   ├── rules.json            ← 合并视图（单一事实来源，当前 191 条，带 模块 字段）
│   ├── rules/                ← 按模块拆分（由 build.py 生成，勿手改）
│   │   ├── build.py          ← 模块化构建脚本（补 模块 字段 + 拆分）
│   │   ├── character_creation.json  (创建调查员 19)
│   │   ├── skills.json             (技能 10)
│   │   ├── game_system.json        (游戏系统 20)
│   │   ├── interlude.json          (幕间成长 4)
│   │   ├── combat.json             (战斗 42)
│   │   ├── chase.json              (追逐 30)
│   │   ├── sanity.json             (理智 29)
│   │   ├── magic.json              (魔法 20)
│   │   ├── keeper.json             (主持游戏 9)
│   │   └── appendix.json           (附录 8)
│   └── weapons.json          ← P2 武器数据（已有，无关本轮任务）
├── prompts/                  ← 创建此目录存放 prompt
└── src/                      ← 现有引擎代码
```

## 当前进度（P3 自动提取 session）

> 工作树：`d:/project/ai_coc_p3`，分支 `p3-rule-extract`，请勿窜到其他工作树。
> 用户指令：自主持续工作，决策点前 commit；如有问题用户自行回滚。

### 已完成章节（合计 191 条，HEAD = `2fd666d`）

| 章节 | TXT 页码 | 条数 | commit | 备注 |
|------|----------|------|--------|------|
| 第八章 理智 | 130-143 | 29 | `67c1c9c` + `2fd666d` | 用户版 17 条 + 补遗 12 条（疯狂发作/表VII·VIII/现实认知/恢复/表IX·X 等） |
| 第六章 战斗 | 86-111 | 42 | `959cab7` | `combat_round_structure`/`melee_opposed_check`/`extreme_success_impale`/`armor_damage_reduction`/`major_wound_effect` 等 |
| 第五章 游戏系统 | 72-85 | 20 | `8cf0379` | 其中 4 条划入"幕间成长"模块 |
| 第七章 追逐 | 112-129 | 30 | `7224fa5` | `action_points`/`chase_hazards`/`breaking_obstacles`/`table_v_vehicles` 等 |
| 第四章 技能 | 44-70 | 10 | `491fe74` | `skill_difficulty_thresholds`/`dodge_half_dex`/`transferable_skill_advantage` 等 |
| 第三章 创建调查员 | 24-43 | 19 | `ddcb168` + `2fd666d` | +1 条（表II 现金与资产） |
| 第九章 魔法 | 144-152 | 20 | `b394cd5` | `spellbook_reading_check`/`cast_spell_push`/`becoming_believer` 等 |
| 第十章 主持游戏 | 153-175 | 9 | `2fd666d` | 新增章节：灵感检定/NPC幸运池/洞察·线索/使用规则·房规 |
| 第十六章 附录 | 356-392 | 8 | `4d8860e` | `death_single_exceed_max_hp`/`outnumbered_bonus_die` 等 |

**全部 9 章已提取完成（含原 8 章 + 第十章主持游戏），合计 191 条，验收标准远超限值。**

> 说明：第十六章附录主体为术语表与物价/武器价格表（已由 P2 `weapons.json` 覆盖），故仅提取 8 条该章**独有**机械规则；恐惧/躁狂症状表（表IX/表X）实际位于第八章理智范围内（lines 9231/9380）。
> 第十一章~第十四章（法术/神话典籍/怪物/外星科技）为**数据型词条**，按用户决定不收录（与 P2 `weapons.json` 同属数据层）。

### 提取方法论（每章统一流程）

1. 用 `grep -n "===== 第 N 页 ====="` 定位章节起止行号（注意：书页号 = TXT 标记页码 - 1）
2. 分块 `Read` 规则书文本（每次 280-470 行）
3. 提取为临时文件 `data/_chX_temp.json`（数组）
4. 用 Python 合并脚本追加到 `data/rules.json`（base + 新，按 `id` 去重）：
   ```python
   import json
   base = json.load(open('data/rules.json', encoding='utf-8'))
   new  = json.load(open('data/_chX_temp.json', encoding='utf-8'))
   ids  = [x['id'] for x in base]
   merged = base + [x for x in new if x['id'] not in ids]
   json.dump(merged, open('data/rules.json','w',encoding='utf-8'),
             ensure_ascii=False, indent=2)
   ```
5. 删除 `_chX_temp.json`，`python -c "import json;json.load(open('data/rules.json',encoding='utf-8'))"` 校验
6. `git add data/rules.json && git commit -m "feat(P3): LLM rule extraction - Chapter X ..."`
7. **注意**：可能存在并行提交者（用户/进程）会先提交合并结果。若 `git commit` 报 `nothing to commit`，先 `git log` 确认 HEAD 是否已含本次合并，再决定是否需要重做。

### 模块化组织（按用户要求：幕间与战斗等分开收录）

> Task #7 已完成。规则按"模块"分类，既保留合并视图 `data/rules.json`（单一事实来源），又拆分为 `data/rules/<module>.json` 便于按主题检索/维护。

**模块划分（字段 `模块` 值 / 文件名 key / 来源）：**

| 模块 | 文件 | 条数 | 来源 |
|------|------|------|------|
| 创建调查员 | `character_creation.json` | 19 | 第三章 |
| 技能 | `skills.json` | 10 | 第四章 |
| 游戏系统 | `game_system.json` | 20 | 第五章（核心检定系统） |
| 幕间成长 | `interlude.json` | 4 | 第五章中"幕间/技能增长/背景演变"：`interlude_growth`/`modify_background`/`interlude_employment_cr`/`training` |
| 战斗 | `combat.json` | 42 | 第六章 |
| 追逐 | `chase.json` | 30 | 第七章 |
| 理智 | `sanity.json` | 29 | 第八章 |
| 魔法 | `magic.json` | 20 | 第九章 |
| 主持游戏 | `keeper.json` | 9 | 第十章 |
| 附录 | `appendix.json` | 8 | 第十六章 |

**构建方式：** `data/rules/build.py` 读取 `rules.json`，按 `章节` 补 `模块` 字段（少数 Ch5 规则覆盖到 `幕间成长`），回写合并视图，再拆分输出各模块文件。**新增/修改规则只需编辑 `rules.json` 后重跑 `python data/rules/build.py`**，勿手改模块文件。

```bash
python data/rules/build.py
# 校验
python -c "import json,glob,os; [json.load(open(f,encoding='utf-8')) for f in glob.glob('data/rules/*.json')]; print('OK')"
```

### 待提取章节（按优先级）

> ✅ 全部 9 章已提取完成（原 8 章 + 第十章主持游戏）。无需再提取规则条目。

| 优先级 | 章节 | TXT 页码 | 状态 |
|--------|------|----------|------|
| 9 | 第十章 主持游戏 | 153-175 | ✅ 已完成 (`2fd666d`) |
| — | 第十一章~十四章（法术/典籍/怪物/科技） | — | ⏭️ 用户决定不收录（数据型词条，同 P2 数据层） |

### 待确认事项

- 残留未跟踪文件 `data/ch6_combat.txt` 经核查**不存在**（已确认 `git status` 干净、目录无此文件）。

---

## 开始第一步

1. 阅读 `data/守秘人规则书.txt` 第 130-143 页（第八章 理智）
2. 按上述格式提取所有机械规则
3. 输出到 `data/rules.json`
4. 发回成果让我在主分支 review

---

## 后续可选项（全部章节提取完成后的增强方向）

1. **去重与一致性复查**：跨章节检查重复/冲突规则（如 `combined_skill_check` 已在 Ch4/Ch5 去重，确认其他无遗漏）。
2. **标签检索验证**：确认每条规则带 1-5 个标签，便于 `grep` 按关键词（理智/战斗/魔法/属性）检索。
3. **补充遗漏机制**：视需要回溯第八章范围（lines 8678-9921）内的表IX恐惧症状表、表X躁狂症状表（目前未单独提取为规则条目）。
4. **格式校验**：`python -c "import json;json.load(open('data/rules.json',encoding='utf-8'))"` 确认 JSON 可解析（已通过）。

---

# Hy3 怪物数据提取任务（NEW）

## 工作树

```
d:/project/ai_coc_hy3/       ← 你在这里（分支 hy3-monsters）
├── data/
│   ├── 守秘人规则书.txt      ← 1.5MB
│   └── ch14_raw.txt          ← 第十四章原始文本（已提取，101KB）
├── prompt.md                 ← 本文件
└── .venv/                    ← 已配置
```

## 目标

从规则书第十四章（怪物、野兽和神话诸神，txt 第 240-313 页）提取所有怪物数据为 `data/monsters.json`。

十四章节分四个子部分：
- **14.1 神话生物** — 异形怪物（拜亚基、空鬼、古老者…约 30 只）
- **14.2 神话神灵** — 旧日支配者/外神（克苏鲁、奈亚…约 10 只）
- **14.3 经典怪物** — 恐怖电影经典（木乃伊、吸血鬼、狼人、丧尸等，约 6 只）
- **14.4 野兽** — 普通动物（蝙蝠、熊、鳄鱼、狼等，约 12 只）

**已识别 47 个属性块**，格式为 "属性 平均 掷骰"。

## 提取方法

1. 逐块读取怪物文本（每个怪物名称行 → 到属性表 → 到技能/护甲/理智损失），每次 Read 250-400 行
2. 提取为结构化 JSON
3. 按 `python data/rules/build.py` 方式：写入临时文件 → 合并到 `data/monsters.json` → 去重并 commit

## 首次执行需重建临时文件

```
python -c "
with open('data/守秘人规则书.txt', encoding='utf-8') as f:
    data = f.read()
idx = data.find('===== 第 240 页 =====')
idx15 = data.find('===== 第 314 页 =====')
with open('data/ch14_raw.txt', 'w', encoding='utf-8') as f:
    f.write(data[idx:idx15])
"
```

ch14_raw.txt 行号 == txt 行号，直接 Read 定位。

## 输出格式

每个怪物一条 JSON，参考已有规则数据格式：

```json
{
  "id": "byakhee",
  "名称": "拜亚基",
  "别名": "星骏",
  "分类": "神话生物",
  "属性": {
    "STR": 90, "CON": 50, "SIZ": 90, "DEX": 67, "INT": 50, "POW": 50
  },
  "掷骰": {
    "STR": "5D6×5", "CON": "3D6×5", "SIZ": "5D6×5", 
    "DEX": "3D6+3×5", "INT": "3D6×5", "POW": "3D6×5"
  },
  "HP": 14,
  "伤害加值": "1D6",
  "体格": 2,
  "魔法值": 10,
  "移动": "5/16 飞行",
  "每回合攻击": "2次",
  "战斗方式": "爪子攻击或撞击",
  "格斗": 55,
  "格斗_伤害": "1D6+DB",
  "闪避": 33,
  "护甲": "2 毛发与坚韧兽皮",
  "技能": "聆听50%，侦查50%",
  "法术": "有40%的几率拥有1D4种法术",
  "理智损失": "1/1D6",
  "特殊能力": ["飞行：携带一名骑乘者穿越宇宙（需魔法或药水保护）"],
  "来源章节": "第十四章 怪物、野兽和神话诸神"
}
```

**字段说明：**
- `id`: 英文拼音标识
- `名称`: 中文名
- `别名`: 逗号后的正式称呼（可省略）
- `分类`: "神话生物" / "神话神灵" / "经典怪物" / "野兽"
- `属性`: 六维数值
- `掷骰`: 对应的生成骰
- `HP` / `伤害加值` / `体格` / `魔法值` / `移动`: 基本战斗数据
- `每回合攻击` / `战斗方式` / `格斗` / `闪避`: 攻击数据
- `护甲` / `技能` / `法术`: 防御及特殊能力
- `理智损失`: 见到怪物的理智损失
- `特殊能力`: 列表形式，提��非数值的独特机制
- `来源章节`: 固定值

## 注意事项

1. **分类**：根据怪物所在小节判断；神灵（克苏鲁/奈亚拉托提普等）为 "神话神灵"
2. **属性缺失**：神灵常不给出掷骰，只给固定值（如 克苏鲁 STR=700），没掷骰可省略 `掷骰` 字段
3. **多形态怪物**：如 乌波-萨斯拉 的子嗣/修格斯形态，应单独条目
4. **野兽章节**：格式稍有不同，不总有 "Name，Title" 模式
5. **中文引号/连字符**：保留原文书写（·、—、～等），不要转换
6. **commit 规范**：每完成一个子章节 commit 一次
7. **去重**：追加前检查 `id` 是否已存在
